# -*- cperl -*-
eval 'exec $CBROOT/bin/perl $0 ${1+"$@"}'
  unless $running_under_some_shell;
use strict;

use NP::Model;

my $dbh = NP::Model->dbh;

for (1..5) {
  my $rv = $dbh->do(q[delete from log_scores where ts < date_sub( NOW(), interval 365 day) limit 7500]);
  last unless $rv;
}

