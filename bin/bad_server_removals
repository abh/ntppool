# -*- cperl -*-
eval 'exec $CBROOT/bin/perl $0 ${1+"$@"}'
  unless $running_under_some_shell;
use strict;
use warnings;

print <<'EOF';
This script has been replaced by the Go serverremoval task.

The server removal functionality has been migrated to:
  go/ntp/api/tasks/serverremoval

The task runs automatically every 24 hours as part of the API service.

If you need to manually trigger server removals, use the Go API instead.
EOF

exit 0;
