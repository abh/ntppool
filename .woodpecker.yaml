when:
  - event: [push, pull_request, manual, tag]

clone:
  git:
    image: woodpeckerci/plugin-git
    settings:
      tags: true

steps:
  - name: submodules
    image: alpine/git
    commands:
      - git submodule update --init --recursive

  - name: generate-tags
    image: ghcr.io/abh/woodpecker-docker-tags-plugin:0.2.1-abh
    settings:
      tags: |
        branch
        sha
        edge -v latest
        raw -v build-${CI_PIPELINE_NUMBER}
        semver --auto

  - name: docker
    # :latest (2.3.2) ships kaniko v1.27.6, which panics on VOLUME with
    # cache enabled (osscontainertools/kaniko#793). Remove :next once
    # plugin-kaniko 2.3.3 or later has been released.
    image: woodpeckerci/plugin-kaniko:next
    settings:
      registry: harbor.ntppool.org
      repo: ntppool/ntppool
      cache: true
      username:
        from_secret: harbor_username
      password:
        from_secret: harbor_password
    depends_on: [submodules, generate-tags]
