[%- page.title = template.title;
    page.style = template.style or 'default.html';
    page.js    = [];
    page.css   = [];
    js_final   = "";

    # don't include any whitespace from here...
    RETURN;
-%]